# CURRENT state

**Last updated:** 2026-09-21 (settlement desk chrome + name-only; tunnel security gate BLOCK; N-0030 backlog audit + staged work plan)

**Branch:** `feat/ns-2-brief-nav-collapse`

**Last content pin:** N-0030 backlog audit + staged plan `8a94e55`. Settlement desk FundingChrome + name-only `492795c`. Grid parity unit 1 `b9a58a3`; grid/column parity audit `d0a9936`; density proposal `b0db502`. Prior grid parity: column pickers `306b17d`, steward/ops/users `47ef8bb`, sell-out/channel-ops/CST `0d65000`, density lab `b0c0112`. Settlement desk single CTA + DualMoney follow-ups `b6f6379`. Line-identifier preference finish `8ff8dc6` (series `baaf485`→`8ff8dc6`). FX booked-at-create + DualMoney `8b038a1` (lab parity `f1032e7`). Settlement desk product `b1696b0`. Lab A shared mount `afc694f`. Prior: StewardQueueOverview D-0011 copy `3f101a9`. Settlement lab A/B `daf0503`. Ledger `19f26f8`.

**Alembic (code):** `20260906_0022` (`cpor_case.intelligence_exclude`)

**Alembic on cip:** `20260906_0022`

## On feat/ns-2-brief-nav-collapse

- **2026-09-21 N-0030 backlog audit + staged work plan (never main; N-0030 `in_progress/validate`, NOT complete — Warren reviews):** New EIF discovery node **N-0030** (run `BACKLOG_AUDIT_20260921`, actor `backlog-audit-001`, R1, created with explicit `--id` because `ids.node` reads **5** vs 29 nodes). Evidence `EV-STK-31712DF` (`.eif/audit/BACKLOG_AUDIT_20260921/BACKLOG_AUDIT.md`) and `EV-STK-EB37AD5` (`docs/design/STAGED_WORK_PLAN.md`). **All 100 open BACKLOG entries classified** against tree + read-only `cip`: 7 DONE (130, 134, 138, 142, 066, 073, 013), 6 SUPERSEDED (149–154 — six-container IA replaced by N-0013), 3 DROP proposed (193 paid licence breaks R0 + N-0029 AC; 014; 017), 2 re-homed to P6 (009, 010), 1 **HELD** (200 vs N-0029 AC "do not unify MasterColumnPickerDialog/ColumnSelectorModal"), 12 EIF-repo out of CIP scope, 30 NECESSARY, 39 HELPFUL → **31 of 100 are not CIP build work**. 46 Status cells stamped in `docs/BACKLOG.md`. **Corrections:** P1 **exited 2026-08-01** (ROADMAP:450, CONTEXT:438) — nothing for Warren to sign; BACKLOG-034 inverted `dim_product` windows **2,795** (was 319); cases 39/40 gone (repaired via supersession); `tsc` clean; BACKLOG-077 shipping mailbox slice **live** (`mailbox_ingest_runner` in `main.py`). Measured: `cpor_case` status/workflow drift **4** rows; `fact_inventory_customer` **0** vs `fact_customer_sellthrough` **1,823**; `cpor_claim_evidence_line` **0** vs **211** settled; `fact_competitor_price` **0**; 14 TMP-coded active customers of 4,949 (`dim_customer.code`); `customer_leftover_repair.py` has zero callers; `market.py:12` still lies `competitor_price_import: ready`; `LineupScopeBar.tsx:106` inert Apply. **Staged plan** (draft, Wednesday revision expected): stages 0–10; Stage 1b (N-0029 GOV-008) deliberately after Stage 2 (density) so `grid_above_fold` is reviewed once; 7 decisions in §3 — first is that grid-type-token option (a) edits `packages/ui`, **outside `change_paths`**. Incident: analysis imports wrote `.eif/runtime/programme/__pycache__` → `RUNTIME_INTEGRITY`; removed (untracked, gitignored); `PYTHONDONTWRITEBYTECODE=1` since. Warren decisions 2026-09-21 recorded in plan §0: fresh independent GOV-008 both nodes; density 40/40+13px; **all columns** pickable; merge 5 orphaned CPOR tabs onto desk; build features 1–9, CST backfill excluded (Warren sources data).
- **2026-09-21 settlement desk chrome + tunnel gate (`492795c`):** `/commercial-planner/cpor-cases/[id]` was the only funding route without `FundingChrome` — h1 at x=252 flush to rail, 0 gutter (predecessor `CporCaseWorkspace` HAD the chrome; dropped in the Composition A swap). `FundingChrome` gains `hideDomainHeader`; desk keeps its case header; gutter 0→20, lens tabs back, Case book active. **`SHOW_CUSTOMER_CODE=false`**, `codeMeta` removed — codes only in a pickable code column (Warren, repeated). Desk gate → `ModuleDataSection` (caught my own `!data`-during-load bug in browser, not tests). 689/689, tsc 0, browser case 46. **Cloudflare Quick Tunnel task: Phase 2 BLOCK** — `apps/api/.env` is **0 bytes**, effective `cip_auth_mode=stub` → unauthenticated = `admin@local` ADMIN, headers forgeable, on real `cip` data. Real login exists (PBKDF2 260k, 68 `auth_session` rows, `redirectToLoginOn401`). Fix = `CIP_AUTH_MODE=session` + Warren sets passwords + `next build && next start`; single entry :3000 (same-origin proxy to loopback :8001); **no SSE anywhere**; 34/58 endpoint modules lack `tenant_id`. Nothing installed, no tunnel started.

- **2026-09-21 grid parity close-out + two audit docs (never main, no GOV-008, no node complete):** **`gridOptions.loading` is fully closed** — no production grid passes it. `b9a58a3` migrated the last three: **commercial planner** lines grid (was an AG Grid overlay while pending and, with no plan picked, a blank bordered box with no copy; lines query's `isError`/`error` were destructured nowhere so a failed load was silent) → `ModuleDataSection` with two distinct empties ("No plan selected"/New plan vs "No lines in this plan"/Add line + Add from lineup); **`CporPaymentEvidencePanel`** info-Alert empty → `EmptyWorkspace` with Import Center + payment-wizard CTAs, error gains Retry; **`CstArticleAliasesSection`** `loading:` was dead since `0d65000` (MDS never mounts the grid while loading) — removed with the reason in a comment. `page.test.tsx` MDS mock was a bare passthrough that would have made the loading-overlay test vacuous; now honours `isLoading`. **`pnpm test:web` 688/688 (154 files). `tsc --noEmit` on `@cip/web` is CLEAN — the pre-existing failures this file recorded (duplicated import block in `lineup/cases/page.test.tsx`, MUI `slotProps` typing) no longer reproduce.** eslint clean on changed files. Browser: planner stash A/B on the same warm tree — before, an empty bordered box with no text; after, "No plan selected · [New plan]". CST steward > Master data > Article aliases renders rows with FLAG chips + Edit/Confirm/Reject intact. **`CporPaymentEvidencePanel` UNABLE to render — `CporCaseWorkspace` is unmounted** (only referent is `page.fxReadiness.test.tsx`; the `[id]` route renders `SettlementDeskLive`); **third orphan after `SettlementPortfolioRead` and `CporPortfolioIntelligencePanel`** → BACKLOG-202. `cip` has **zero planner plans** (`GET .../plans` → `[]`), so the populated grid and per-plan empty were not reachable without writing to `cip` — not done; covered by vitest. Earlier "Loading plans…" that looked stuck was **auth/tenant warm-up, not a bug** — it resolved to "No plans yet" once the session established.
- **2026-09-21 audit docs:** `docs/audits/GRID_AND_COLUMN_PARITY_AUDIT.md` — 63 `EnterpriseDataGrid` mounts (53 production, 10 design-lab) across 53 files, 3 `MasterDataGridShell`, 41 MUI-`<Table>` files of which 23 own a `useQuery`, 5 grid files with a column picker. **Zero direct `AgGridReact` mounts outside the wrapper.** All 14 production grid files lacking a local `ModuleDataSection` classified against five stated CORRECT criteria. **One MIGRATE remains: `MarketSurface` competitor-mappings grid (`:1230`)** — query at `:272` destructures neither `isLoading` nor `isError` → BACKLOG-199. ~46 grid mounts have **no** column picker, split Tier A (candidate gap, product call) / B (fixed set is the design) / C (case panels). `docs/design/DENSITY_PROPOSAL_OPERATOR_DATA_SCALE.md` — **PROPOSAL ONLY, NOT SHIPPED.** Verified in browser at `/design-lab/density`: 11 → **14** rows in the same 540px grid (+27%), and the product column stops truncating at 13px (headers too). Three findings: a **density axis already exists** (`cipTheme.ts` comfortable/compact, live toolbar toggle + settings + `uiStore`) and proposed comfortable 36/36 sits **2px from compact 34/36 with an identical header height**, so the toggle would look broken — Warren's call; setting `body2.fontSize` in `cipTheme.ts` is **not grid-scoped** (721 `variant="body2"` usages); the four height numbers live in **three places** (`EnterpriseDataGrid.tsx:121-122`, `gridPagination.ts:7-10`, and dead in `packages/ui/agGridMuiTheme.ts:78-79` — explicit props win). Change path is `apps/web` inside `change_paths`; **`packages/ui` is not where the live values live**.

- **2026-09-20 settlement desk follow-ups (never main, no GOV-008, no node complete):** `SettlementDesk.test.tsx` 3/3 green again — wrapped in `QueryClientProvider` + `@/lib/api` mock (CaseBookSurface convention) after `baaf485` added `useLineIdentifierPreference` (useQuery). Desk primary CTA renders **once** (header); Next-action panel dropped its duplicate button, subtitle names the header CTA. `HeadlineStrip` splits >3 columns to `ceil(n/2)` at md and full count at lg (was 5-up from 900px). Corroboration column already `width 188 / wrapText / autoHeight` and panel already `minmax(240,280)` at md at `8ff8dc6` — nothing to change. `SettlementConfirmDialog` outstanding amount → `DualMoney` (booked USD or honest unbooked; falls back to local when readiness not loaded) + new test. `PlanWorkspace` Total support → `DualMoney` (replaces `≈ $… USD` caption). Browser: case 311 desk one CTA; `/promotions?plan=311` `R 1,616,231.52 / $ 97,953.43 at booked 16.50`. **Unbooked-case HTTP render UNABLE** — all 304 `cip` cases have `fx_declared=true` (FX booked at create); covered by vitest only. `SettlementPortfolioRead` (via `SettlementContainer`) and `CporPortfolioIntelligencePanel` are **unmounted** — USD-primary local `fmtUsd/fmtZar`, backend Σ stored per-line `ttl_support_usd` (booked, not live; None→0). Left untouched. Next dev server wedged (2.3 GB, no response) and was restarted.
- **2026-09-19/20 line-identifier preference (`baaf485`…`8ff8dc6`):** tenant `line_identifier_preference` (`sku` | `sales_model`) via `useLineIdentifierPreference` → `/api/v1/auth/tenant-commercial-profile`; applied on profile, settlement desk, lineup, cover/inventory/movements, sell-out grids, buy-plans/roadmap, forecasts/pricing/inventory, CPOR case/promo plan/promo load, commercial planner, admin + lineup tables. Cover-distribution 4-tuple crash fix `9329e03`.
- **2026-09-18 settlement desk resume (never main, no GOV-008, no node complete):** Composition A is production at `/commercial-planner/cpor-cases/[id]` (`SettlementDeskLive`). Lab A mounts the same `SettlementDesk`. Salvaged CIP product-grain / customer `result_qty` mapping kept. Case 311 SETTLED, 18 lines, 0 claim-evidence rows, customer qty empty — real signal. Name-primary customer display (`SHOW_CUSTOMER_CODE`). Stub auth resolves `admin@local`; settle stamps `decided_by`; historical apply stamps `apply_actor`. PM per-line maps in `column_mapping_memory.mapping_profile.by_product_line`; duplicate EAN FLAG. HTTP API `:8001` 200, web `:3000` 200. Playwright MCP rendered case 311 (name Takealot, not welded CUST-000012). `browser_resize` **not in catalog** — 390×844 UNABLE. N-0028/N-0029 still `in_progress`/`validate`. Handover `docs/design/gov-008-n0028-n0029-handover.md`.
- **2026-09-18 investigate/scope/design (never main, no GOV-008, no node complete):** BACKLOG-198 catalogue is live; EAV write is gated off (`pm_write_legacy_eav` default False → `write_attribute_values=False` in `pm_commit_catalog.commit_catalog_and_eav`). `cpor_case` has `ended`/`settled`, **no paid/closed column** — report migration, do not run. Settlement lab compositions at `/design-lab/funding?lens=settle` (`alt=a|b`); production `/commercial-planner/cpor-cases/[id]` untouched. Specs: `docs/EXTERNAL_API_OUTBOUND.md`, `docs/EXTERNAL_API_INBOUND.md`. StewardQueueOverview leftover D-0002 copy aligned to D-0011 (`3f101a9`). Playwright MCP / `cursor-ide-browser` **not in this session’s tool catalog** — 1280×800 and 390×844 renders UNABLE_TO_RENDER; HTTP 200 on both lab URLs is not smoke.
- **2026-09-18 prior build session:** BACKLOG-192 `ColumnPickerDialog` size md/wide `7656f67`. BACKLOG-197 mappings queue copy `39f457d`. Start work ActionCards paper/icons/approved copy `d25a927`. Item 4 per-line column sets **stopped** → BACKLOG-198.
- **GOV-008 N-0027:** Independent review `GOV008_N0027_20260916` / `gov-022`. Verdict **VERIFIED_WITH_LIMITATIONS**. Node **complete**. Mapping-page restore-vs-retire copy Done (`39f457d`). StewardQueueOverview second instance now Done (`3f101a9`).
- **D-066 locked:** steward and mapping resolve inside `/admin/mappings?workspace=resolve`. Do not page-hop. D-0011 KEEP: `entity_mapping_queue` is pipeline state, not a UI to restore or retire.
- **NS21:** D-0011 KEEP (path C) accepted; supersedes D-0006 and D-0002. N-0027 AC1 rewritten (D-0011). Handover: `docs/design/gov-008-n0027-n0029-handover.md`.
- **NS20:** N-0028 `verification.rendered` / `verification.referent` reset pending. N-0029 `quality.design_signatures` reset pending.
- **Programme:** PRG-20260831T145514. **N-0018–N-0027 complete.** **N-0028, N-0029** `in_progress` / `validate` (PROGRAM.yaml). Generated `.eif/CURRENT.md` showing empty frontier is **stale vs PROGRAM.yaml** — tree nodes win. Do not reopen N-0013.
- **D-0010 accepted** Option A: keep rail expansion. Rail and tabs are not one destination set.

**Mobile:** DIRECTION §6 desktop-primary with named 390px workflows. N-0025 Start work VERIFIED at 390×844. This session: Playwright MCP rendered case 311; `browser_resize` not in catalog so 390×844 UNABLE. Do not use `browser_cdp`.

**Known pre-existing:** none outstanding on `@cip/web`. The `tsc --noEmit` failures previously recorded here (duplicated import block in `lineup/cases/page.test.tsx` TS2300; MUI `slotProps.input` `data-testid` typing in `CporCaseWorkspace`, `SemanticCatalogOverlayPanel`, `ShippingDigestRecipientsPanel`) **no longer reproduce** — `tsc --noEmit` exited 0 on 2026-09-21 at `b9a58a3`. eslint: 0 errors, 54 pre-existing `react-hooks/exhaustive-deps` warnings. `pnpm lint` needs `ESLINT_USE_FLAT_CONFIG=false`.

**Next:** Warren reviews `docs/design/STAGED_WORK_PLAN.md` §3 (7 decisions) + Wednesday additions → then charter Stage 2 (grid/desk/density) and Stage 3 (auth→RBAC→tunnel) as parallel nodes; **N-0028 GOV-008 by a distinct actor** (`docs/design/gov-008-n0028-n0029-handover.md`); **N-0029 GOV-008 only after Stage 2** lands. Tunnel: `CIP_AUTH_MODE=session` + passwords first. Ken/PM/Wayne role matrix (BACKLOG-136). Customer code display **RESOLVED** name-only 2026-09-21. Sellable grain five vs sixteen (BACKLOG-198). Paid/closed future migration.

**Design language:** Production follows implemented design-lab React. Do not cite a frozen design-language version or grammar number.

**Deferred:** See `docs/design/STAGED_WORK_PLAN.md`. Audit 2026-09-21: 193 **dropped**, 149–154 **superseded**, 200 **HELD**, 009/010 → P6, 014/017 drop proposed. BACKLOG-199 `MarketSurface` mappings grid (only open grid MIGRATE). BACKLOG-200 `MasterColumnPickerDialog` dead test-only wrapper. BACKLOG-201 Tier A column-picker coverage (product call). BACKLOG-202 `CporCaseWorkspace` unmounted — third orphan. BACKLOG-174–180. BACKLOG-181 token port done; IA settled D-0010 Option A. BACKLOG-183–186 N-0026 UNCOVERED. BACKLOG-187/188 N-0027. BACKLOG-190 lineup authoring workbench. BACKLOG-191 desk A on production (`b1696b0`); paid/closed still future. BACKLOG-193 AG Grid Enterprise. BACKLOG-197 Done. BACKLOG-198 partial (JSON line maps; 5 vs 16 grain still Warren). BACKLOG-136 remainder Ken/PM/Wayne roles. BACKLOG-140 remainder: code-on-desk vs name-only; do not bulk-promote TMP. BACKLOG-173. Budget ledger writer not chartered. Leftover `/market` stub. Stores master grid UNCOVERED. Cross-job steward accept/reject until Design Language v2. Pin-as-widget on Overview UNCOVERED. `/dashboard` legacy UNCOVERED. Paid/closed on `cpor_case` is a future migration.

**Env:** local Windows. Web `:3000` + API `:8001`. Sync/async engine on `cip` (`current_database()=cip`).
