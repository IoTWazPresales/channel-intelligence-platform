# Independent GOV-008 — N-0019, N-0020, N-0021, N-0022

**Run:** `GOV008_N0019_N0022_20260913`  
**Actor:** `gov-008` (this session; not the implementer)  
**Date:** 2026-09-13  
**Mode:** implementation-verification + evidence-skeptic  
**Reviewer HEAD:** `cc51e6a` (`feat/ns-2-brief-nav-collapse`)  
**Programme:** PRG-20260831T145514, views at review start claimed rev 435  

Do **not** reuse implementer runs `NS10_OVERVIEW_20260912`, `NS10_PLANNING_20260912`, `NS10_ADMIN_20260913`, `NS10_STOCK_LEAVES_20260913` or actor `gov-001`.

Out of scope (not defects of these nodes): D-0002, N-0006, N-0013, navigation rail/tab IA, BACKLOG-181 LabShell rail.

Judge production against **lab composition**, not LabShell. Frozen design-language version / grammar numbers are not cited.

Every claim below is **VERIFIED** (this session’s artefact, SQL, API, or Playwright) or **ASSERTED** (inference).

---

## Independence rung

**R2 session separation + programme provenance.** Fresh GOV-008 context. Own Playwright MCP session (`browser_resize` 1280×800; N-0019 Attention also 390×844). Own read-only SQL with `current_database()` printed first (`cip`). Own live `GET` of overview APIs. Own lab SOURCE read before coverage-map reconcile. **ASSERTED:** no second-LLM consult (same limitation N-0018 recorded as non-blocking). Planted-false on hashes is mechanical: product commits exist.

`browser_cdp` was not used.

---

## Product pins (FACT — commits exist)

User named N-0019 product `e8bcdeb`. That commit is the **docs pin**. The Overview **mount** is `c1b4311`.

| Node | Implementation run / actor | Mount commit | Docs pin |
|---|---|---|---|
| N-0019 | `NS10_OVERVIEW_20260912` / gov-001 | `c1b4311` overview mount | `e8bcdeb` |
| N-0020 | `NS10_PLANNING_20260912` / gov-001 | `ce88549` planning mount | `77da2db` |
| N-0021 | `NS10_ADMIN_20260913` / gov-001 | `fec7d0e` administration mount | `b0c6cab` |
| N-0022 | `NS10_STOCK_LEAVES_20260913` / gov-001 | `712d9d8` leftover ThinLens | `5a7bf00` |

`git diff c1b4311^..712d9d8 -- apps/web/src/features/shell/navConfig.ts` is empty. **VERIFIED:** rail/tab IA files were not changed by these four product mounts.

---

## NUMBER RULE (this session)

Script: `docs/design/gov008_n0019_n0022_numbers.py`. Engine: sync URL via `sqlalchemy_sync_engine_url`. **VERIFIED:** `current_database()=cip` printed first. No writes.

Live APIs on `:8001` also returned `"database": "cip"` on Planning / Administration / leftover-stock payloads.

### N-0019 Overview

| Signal | Grain | This session | Class |
|---|---|---|---|
| Brief signals | `GET /api/v1/brief/signals` (authenticated browser) | **3** — failed_imports **45**, cover_breach **110**, inbound_open **1964** | (iii) **VERIFIED** on `/brief` |
| Dashboards (SQL tenant) | `dashboard` rows tenant `default` | **3** | (iii) SQL **VERIFIED** |
| Widgets (SQL tenant) | `dashboard_widget` | **6** | (iii) SQL **VERIFIED** |
| Saved reports (SQL tenant) | `saved_report` | **3** | (iii) SQL **VERIFIED** |
| Dashboards (anonymous API) | list visibility filter | **2** published (`test` 1 widget, `Channel stock board` 1 widget) | different grain **VERIFIED** |
| Hub canvas | `dashboards[0]` in the signed-in browser | **test · 1 widget** | (iii) **VERIFIED** |
| Pinned reports (signed-in UI) | hub `pinned-reports` | 3 personal reports | (iii) **VERIFIED** — matches SQL 3 |

Lab widget KPIs (SOH, 1 714 shipments, funding outstanding, W36 sell-out) were **not** copied. **VERIFIED.**

Implementer coverage map recorded **43** failed imports on 2026-09-12. This session’s live blotter is **45**. **VERIFIED** NUMBER RULE: do not force agreement across time.

### N-0020 Planning

| Headline | Lab fixture (i) | cip this session (iii) |
|---|---|---|
| Lineup cases | 14 | **29** |
| Plan lines caption | 1 260 | **2703** |
| Plan units | 96 400 | **273982** |
| Shipped vs plan | 74% fixture | **20%** fill rate, Execution vs plan **26Q3**, 6586 / 32509 |
| Lines not ready | 158 | **2703** |
| Economics flagged | 280 | **0** (0 `commercial_plan_line` rows) |

On-screen `/lineup` matched the API payload. **VERIFIED.** Lab 14 / 96 400 / 74% / 158 / 280 were not copied.

### N-0021 Administration

| Headline | Lab fixture (i) | cip this session (iii) |
|---|---|---|
| Users | 14 | **2** (1 admin · 0 steward · 0 planner · 1 viewer) |
| Jobs running | 2 | **0** (64 pending, not running) |
| Failed 24h | 1 | **1** |
| SQL 7d | 38 | **0** (8 all-time) |
| Failed still open | — | **45** (ops workspace; not the 24h headline) |

Implementer coverage map recorded failed 24h **0** / failed open **44**. This session: **1** / **45**. **VERIFIED** time grain, not a composition defect.

### N-0022 leftover stock leaves

| Gate | Lab fixture (i) | cip this session (iii) |
|---|---|---|
| Current ISO week | W36 | **2026-W37** (today SAST 2026-09-13) |
| CST this week | W36 not imported | **0** rows Monday..+6d |
| Latest CST | TechMart W35 | **2026-09-28 / W40**, 75 rows, Computer Mania 56 · Game 19 |
| Sell-out trailing 8 | fixture copy | **0 of 8**; latest **2026-06-12 / W24** |
| Forecast rows | not a working lens | **38625** on relocated workspace only |

On-screen ThinLens titles matched the API. **VERIFIED.** TechMart / W36 not copied.

---

## Own route enumeration (before coverage-map reconcile)

### N-0019

Lab SOURCE: `OverviewSurface.tsx` — DomainHeader, two-column Business dashboard ∥ Needs attention + Pinned reports; `attentionFirst` when mobile and `?zone=attention`. ReportsSurface at `/design-lab/reports` is the reports leaf.

Production:

| Route | Observation this session | Coverage (reviewer) |
|---|---|---|
| `/brief` | OverviewChrome + OverviewHub; two-column at 1280; live signals + tenant dashboard | **COVERED** (structure) |
| `/brief?zone=attention` | 390×844 attention stacked above dashboard | **COVERED** |
| `/dashboards` | OverviewChrome wrap; Add widget / canvas still present | **COVERED** relocate |
| `/reports` | OverviewChrome wrap; governed builder still present | **PARTIAL** vs lab ReportsSurface fixture runner; existing builder relocated |
| `/inbox` | OverviewChrome wrap; production-only leaf | **COVERED** wrap |
| In-place Edit/Add on hub | Edit → `/dashboards` | **PARTIAL** |
| Pin-as-widget | honesty copy; list only | **UNCOVERED** |
| Lab fixture widgets as hub KPIs | not copied | **UNCOVERED** (honest) |
| `/dashboard` legacy | not migrated this review | **UNCOVERED** leftover (**ASSERTED** from route inventory; not re-rendered) |
| `navConfig` Overview hrefs | `/brief`, `/dashboards`, `/reports`, `/inbox` | unchanged **VERIFIED** via diff |

### N-0020

Lab SOURCE: `PlanningSurface` DomainOverview — 5 headlines, PairedBars, readiness bars, attention, workflows. Lab page ignores `?lens=`.

Production:

| Route | Observation | Coverage |
|---|---|---|
| `/lineup` | PlanningChrome + PlanningOverview; cip figures; workflows | **COVERED** structure |
| `/lineup/cases` | Relocated LineupContainer; LensTabs on leaf | **COVERED** relocate |
| `/commercial-planner` | PlanningChrome wrap; planner workspace | **COVERED** wrap |
| `/roadmap` | PlanningChrome wrap; empty substrate workspace | **PARTIAL** substrate honesty |
| Lab `?lens=po` / rankings | not stolen onto Planning rail | **PARTIAL** as mapped |
| Rail Planning hrefs | `/lineup`, `/commercial-planner`, `/roadmap` | unchanged **VERIFIED** |
| Workflow Lineup cases | real `href=/lineup/cases` | **VERIFIED** |

### N-0021

Lab SOURCE: `AdminSurface` — 4 headlines, Operations panel, attention, workflows. Lab ignores `?tab=`.

Production:

| Route | Observation | Coverage |
|---|---|---|
| `/admin/users` | AdminChrome + AdminOverview; cip figures | **COVERED** |
| `/admin/users/list` | `data-testid=users-workspace`; directory table | **COVERED** relocate |
| `/admin/ops` | AdminChrome wrap; Failed open 45 | **COVERED** wrap |
| `/admin/sql-viewer` | AdminChrome wrap; audited console | **COVERED** wrap |
| `/settings` | AdminChrome wrap | **COVERED** wrap |
| Platform audit log | “Not in navigation yet: Audit log (planned)” | **PARTIAL** planned honesty |
| `/admin/mappings` | still Data & Stewardship steward queue; D-0002 copy | **untouched VERIFIED** |
| Workflow Users | `href=/admin/users/list` | **VERIFIED** |

### N-0022

Lab SOURCE: `StockSurface.tsx` ThinLens `sellthrough` / `forecast` only. Cover / Movement / Execution out of scope.

Production:

| Route | Observation | Coverage |
|---|---|---|
| `/channel-intelligence` | `sellthrough-honesty` ThinLens W37 | **COVERED** |
| `/channel-intelligence/workspace` | CST grid relocated | **COVERED** |
| `/forecasts` | `forecast-honesty`; no Compute from history | **COVERED** |
| `/forecasts/workspace` | grid + Compute from history | **COVERED** |
| `/stock?lens=sellthrough` | client redirect → `/channel-intelligence` | **COVERED VERIFIED** |
| `/stock?lens=forecast` | client redirect → `/forecasts` | **COVERED VERIFIED** |
| `/stock?lens=cover` | Network cover 27.0 weeks still present | **out of map VERIFIED** untouched |

---

## Rendered comparison (Playwright)

Viewport **VERIFIED:** `innerWidth===1280` `innerHeight===800` after `browser_resize`. Attention **VERIFIED:** `innerWidth===390` `innerHeight===844`.

| Node | Lab | Product | Verdict |
|---|---|---|---|
| N-0019 | `/design-lab` DomainHeader greeting, 9 fixture widgets, Needs attention 5 urgent · 3 informational, Pinned reports | `/brief` DomainHeader **Overview**, tenant dashboard, 3 live signals, 3 pinned reports | composition match with NUMBER RULE substitution |
| N-0019 390 | — | `/brief?zone=attention` kids `attention-zone` then dashboard; attTop 201 < dashTop 826 | Attention-first **VERIFIED** |
| N-0020 | `/design-lab/planning` 14 / 96 400 / 74% / 158 / 280 | `/lineup` 29 / 273 982 / 20% / 2 703 / 0 | composition match |
| N-0021 | `/design-lab/admin` 14 / 2 / 1 / 38 | `/admin/users` 2 / 0 / 1 / 0 | composition match |
| N-0022 | lab Sell-through W36 / TechMart | production W37 / Computer Mania · Game | ThinLens match |

BACKLOG-181 LabShell rail divergence is **not** scored.

---

## Verdicts

### N-0019 Overview — **VERIFIED**

Acceptance criteria met: lab Overview composition mounted on `/brief`; dashboards / reports / inbox relocated not deleted; coverage map exists and matches this reviewer’s enumeration after the fact; NUMBER RULE held; 1280 two-column + 390 Attention-first; Partly built / Planned not faked as pin-as-widget; D-0002 untouched; high_fidelity.

**Limitations (non-blocking):** DomainHeader title is **Overview**, not lab “Good morning — Aurora Displays SA”. Hub Edit is a deep-link (**PARTIAL**). Pin-as-widget **UNCOVERED**. Keyboard/axe not exercised. 312px attention column width claimed by implementer, not re-measured here. R3 second-model consult not invoked.

### N-0020 Planning — **VERIFIED**

Lab Planning DomainOverview mounted on `/lineup`; LineupContainer relocated to `/lineup/cases`; commercial-planner and roadmap wrapped; N-0009 is not treated as the design source; NUMBER RULE held; 1280 only; roadmap stays substrate; D-0002 / rail IA untouched.

**Limitations:** PO reconciliation and rankings remain PARTIAL (not stolen onto Planning rail). Workflow click-through not re-clicked; `href` **VERIFIED**. R3 second-model consult not invoked.

### N-0021 Administration — **VERIFIED**

Lab Admin composition mounted on `/admin/users`; users workspace relocated; ops / sql-viewer / settings wrapped; audit log planned honesty; NUMBER RULE held (live 24h grain **1**, not the coverage-map freeze **0**); 1280 only; D-0002 untouched.

**Limitations:** coverage-map 24h figure is stale; live UI is the grain. Keyboard/axe not exercised.

### N-0022 leftover stock leaves — **VERIFIED**

ThinLens honesty on leftover leaves; workspaces relocated; Cover still Network cover; redirects hold; NUMBER RULE held; 1280 only; D-0002 / rail IA untouched.

**Limitations:** lab Forecast ThinLens was read in SOURCE this session; production Forecast landing was rendered. Lab Forecast page itself was not re-opened in Playwright.

---

## Quality / verification (this run)

All required design_experience dimensions for these nodes are **pass** on the evidence in this file. Delivered class **high_fidelity**. Independent rendered_comparison and design_sameness_review are this run/actor, not the implementer runs.

Engine events that persist those gates are recorded separately via `program.py` (`--payload-file`).
