# Legacy package-YAML overlay (back-compat). Prefer tenant_profiles/{id}.json
# semantic_overlay — that path needs no deploy. This file is still merged through
# the governed field whitelist: label / hidden / allowed_grains only. It cannot
# rewrite formula / source_facts / owner_surface, add dimensions, or invent metrics.
#
# Copy/rename to {tenant_id}.yaml only if you must ship a YAML overlay with the image.
version: 1
metrics: []
dimensions: []
