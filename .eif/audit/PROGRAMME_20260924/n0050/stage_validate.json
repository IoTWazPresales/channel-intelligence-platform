{"node": "N-0050", "expected_revision": 3, "to": "validate", "stage_note": "Implemented at d8b2e7ac: alembic 20260924_0023 (window_start/window_end NOT NULL backfilled from case window as-is; grain uq_cpor_case_line_grain + window_start; CHECK window_end>=window_start; lossless-or-refuse downgrade); writers default to case window; claim rollup uses line windows; straddling weeks FLAGged, never pro-rated. Proven on cip_test only (upgrade/downgrade/upgrade with copied rows, then cleaned). NOT applied to cip; ready-for-cip in IMPL.md. Questions D-0036. Independent review next."}
