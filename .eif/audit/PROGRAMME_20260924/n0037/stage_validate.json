{"node": "N-0037", "expected_revision": 4, "to": "validate", "stage_note": "Implemented at 76316728 (impl subagent, IMPL.md): in-memory per-account (5) and per-address (20) sliding window, 900 s, settings not literals; 429 + Retry-After; lockout refuses the correct password; success resets the account; unknown users verify against DUMMY_PASSWORD_HASH (removes a timing oracle); HMAC-keyed structured logs. 26 tests pass. API restarted; live probe with a fake account: 5x401 then 429, Retry-After 898. Limitation: behind the Next proxy a client-supplied X-Forwarded-For is kept (Next fills XFF only when absent), so per-address limits are bypassable/frameable; per-account limit is address-independent. Counters are per-process and reset on restart. Independent review next."}
