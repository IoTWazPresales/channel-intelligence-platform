{"node": "N-0031", "verdict": "FAILED",
 "dims": {"quality.a11y": {"state": "pass", "summary": "New mappings states are accessible: loading role=status aria-busy with label, error role=alert with named keyboard-reachable Retry, empty role=region with a real Import Center link; lineup ScopeBar is role=toolbar aria-label=Scope, all controls Tab-reachable with handlers and visible focus. Non-blocking findings: weak focus indicator on Import Center (outline none), focus lost to body after Retry, filter chips lack aria-pressed (state by colour only), deferred 'Propose candidates' uses native disabled so its explanation is keyboard-unreachable (pre-existing). Screen reader, zoom/reflow and 390px not tested."},
          "verification.referent": {"state": "fail", "summary": "Criteria 2-8 re-verified (diff, vitest 22/22, tsc 0, eslint 0 errors, pytest 2 passed, DOM: rows/headers 40px on /admin/customers, paginated shells 688/888px on /stock?lens=execution match paginatedGridHeight, mappings empty/loading/error+Retry rendered, ScopeBar has no inert control). Criterion 1 fails: packages/ui/src/agGridMuiTheme.ts:90-91 still emits --ag-row-height/--ag-header-height with its own literals (42px) that now diverge from GRID_DENSITY (40px); present in the live stylesheet (.css-1hv9xw rule) and only masked by EnterpriseDataGrid's inline override."}},
 "failed_criteria": ["1. Single source for grid row and header heights ... packages/ui CSS-variable emission reading the same values or removed as dead : packages/ui/src/agGridMuiTheme.ts:90-91 still emits hardcoded 42px/34px/36px literals (neither reading GRID_DENSITY nor removed); after 459f4c94 they disagree with the single source (42 vs 40) and are present in the rendered CSS, masked only by an inline override"],
 "limitations": ["Compact density not rendered in the browser (would change the operator's saved preference); verified by code/diff only", "5e70f33a's 42/42 build not rendered; running app is post-459f4c94 (40/40)", "Full pnpm test:web not run; 9 relevant vitest files (22 tests) run instead", "Mappings loading/error states induced by client-side fetch stubbing and query reset in the reviewer's tab; tab was visibilityState=hidden so TanStack paused retries and needed manual resume", "No screen-reader, contrast-tool, zoom/reflow or 390x844 testing", "Independence: fresh session, different model from implementer (Opus 5.5 vs Fable 5.1), same vendor family"]}
