<!-- GENERATED by python .eif/runtime/programme/program.py. Do not edit. Authoritative history: .eif/program/PROGRAM_LOG.ndjson -->

# Roadmap (node tree)

- N-0001 **Product/engineering outcomes** `rejected`/`rejected` (feature)
- N-0002 **Outcome 1** `rejected`/`rejected` (feature)
- N-0003 **Dependencies / decision deadlines** `rejected`/`rejected` (feature)
- N-0004 **NS-2 Brief and six-container nav collapse** `complete`/`complete` (redesign)
- N-0005 **TEST criteria probe** `rejected`/`rejected` (discovery)
- N-0006 **NS-1b FX mode and blocked-settle enforcement** `complete`/`complete` (feature) RETROACTIVE
- N-0007 **NS-3 Stock merge — Sell-out, Fill vs plan, Cover and Inbound** `complete`/`complete` (redesign)
- N-0008 **NS-4 CPOR Settlement — queue and case redesign** `complete`/`complete` (redesign)
- N-0009 **NS-5 Lineup plan origination** `complete`/`complete` (redesign)
- N-0010 **NS-6 Actions container (was Response)** `rejected`/`rejected` (redesign)
- N-0011 **NS-7 Data & Stewardship from design-lab** `complete`/`complete` (redesign)
- N-0012 **NS-1a FX display honesty (no migration)** `complete`/`complete` (feature)
- N-0013 **Full-platform IA architecture and buyer vocabulary approval** `complete`/`complete` (discovery) RETROACTIVE
- N-0014 **Promotions & Funding production migration from design-lab** `complete`/`complete` (redesign) RETROACTIVE
- N-0015 **Market & Listings production migration from design-lab** `complete`/`complete` (redesign) RETROACTIVE
- N-0016 **Stock & Sell-through Cover and Movement lenses from design-lab** `complete`/`complete` (redesign) RETROACTIVE
- N-0017 **Stock & Sell-through Execution vs plan from design-lab** `complete`/`complete` (redesign)
- N-0018 **NS-8 Supply & Inbound from design-lab** `complete`/`complete` (redesign)
- N-0019 **Overview production migration from design-lab** `complete`/`complete` (redesign)
- N-0020 **Planning production migration from design-lab** `complete`/`complete` (redesign)
- N-0021 **Administration production migration from design-lab** `complete`/`complete` (redesign)
- N-0022 **Stock & Sell-through Sell-through and Forecasts lenses from design-lab** `complete`/`complete` (redesign)
- N-0023 **Port BACKLOG-181 LabShell rail treatment into production** `complete`/`complete` (redesign)
- N-0024 **Fix label-versus-destination mismatches in nav, brief signals, hub Workflows, and headline figures** `complete`/`complete` (redesign)
- N-0025 **Make actionable work reachable: one Start work surface plus remaining obstruct findings** `complete`/`complete` (redesign)
- N-0026 **Propose a promotion plan from customer and period on the Promotion Planner** `complete`/`complete` (feature)
- N-0027 **Steward queue grouped by failure type, routing into existing engines** `complete`/`complete` (feature)
- N-0028 **Start work cards and Lineup cases on the lab composition** `in_progress`/`in_progress` (feature)
- N-0029 **Grid community clipboard in one wrapper, Case book working content first** `in_progress`/`in_progress` (feature)
- N-0030 **Backlog audit against the running tree, P1 sign-off discovery, and staged work plan** `complete`/`complete` (discovery)
- N-0031 **Stage 2a: grid height single source, MarketSurface mappings states, LineupScopeBar and market readiness honesty** `in_progress`/`in_progress` (feature)
- N-0032 **Stage 2.7: five orphaned CPOR case tabs (USD pivot, Events, Exports, Promo load, Payments/recon) mounted on the settlement desk** `in_progress`/`in_progress` (feature)
- N-0033 **Stage 3.1/3.5: session auth gate on every API route, production web on loopback, Cloudflare quick tunnel for a small pilot** `in_progress`/`in_progress` (feature)
- N-0034 **Stage 2.4 + grid parity** `in_progress`/`in_progress` (feature)
- N-0035 **Stage 2.2/2.3: grid-scoped 13px type token and density 40/40 (ran outside the ledger)** `in_progress`/`in_progress` (feature) RETROACTIVE
- N-0036 **BACKLOG-206: API binds loopback, not 0.0.0.0:8001** `proposed`/`proposed` (feature)
- N-0037 **Stage 3.4: login rate limit and lockout** `proposed`/`proposed` (feature)
- N-0038 **Stage 3.2: CPOR role checks on the existing roles (BACKLOG-136/141)** `proposed`/`proposed` (feature)
- N-0039 **Listing links open the real product page (Market & Listings bug)** `proposed`/`proposed` (feature)
- N-0040 **D-g: sellable BU grain is every dim_product.product_line (five-line assumption sweep)** `proposed`/`proposed` (feature)
- N-0041 **D-b: shared grid search (workbench-ui) on Tier A grids** `proposed`/`proposed` (feature)
- N-0042 **D-b: shared saved grid views (workbench-ui)** `proposed`/`proposed` (feature)
- N-0043 **D-b: shared grid export (workbench-ui, community AG Grid)** `proposed`/`proposed` (feature)
- N-0044 **D-c + rest of 2.7: desk absorbs BACKLOG-202 pieces, CporCaseWorkspace retires, two orphan panels ruled** `proposed`/`proposed` (feature)
- N-0045 **D-d: archive untracked files outside the repo; commit only real source** `proposed`/`proposed` (feature)
- N-0046 **BACKLOG-143: worktrees and leftover-state hygiene** `proposed`/`proposed` (feature)
- N-0047 **Stage 4.1: dim_product inverted launch/retire windows (BACKLOG-034), proven on a clone** `proposed`/`proposed` (feature)
- N-0048 **Stage 4.2: cpor_case status vs workflow_status drift (BACKLOG-139), proven on a clone** `proposed`/`proposed` (feature)
- N-0049 **Stage 4.3: MAC check reads customer sell-through, not the empty inventory fact (BACKLOG-135)** `proposed`/`proposed` (feature)
- N-0050 **Stage 4.4: cpor_case_line week-aligned window columns (BACKLOG-137), migration proven on cip_test only** `proposed`/`proposed` (feature)
- N-0051 **Stage 3.7: import completion asserts no merged-id leftovers (BACKLOG-133/134)** `proposed`/`proposed` (feature)
- N-0052 **Stage 3.6: ops safety net (restore proven on a clone, alerting, resolver fails loudly)** `proposed`/`proposed` (feature)
- N-0053 **Stage 2.5: line identifier 'both' as two columns** `proposed`/`proposed` (feature)
- N-0054 **Stage 2.9: design-lab as fixture skin over production primitives (BACKLOG-161/158)** `proposed`/`proposed` (feature)
- N-0055 **Stage 3.3: tenant scoping sweep** `proposed`/`proposed` (feature)
- N-0056 **Stage 5: analytics delivery (export, event-triggered refresh, calendar delivery, vintage on face)** `proposed`/`proposed` (feature)
- N-0057 **Stage 6.1/6.2: lineup authoring workbench and the B2 end-to-end PM run (BACKLOG-190)** `proposed`/`proposed` (feature)
- N-0058 **Stage 6.3/6.5: unified lineup import 1H fan-out and lineup data rules (BACKLOG-103/105/055/065)** `proposed`/`proposed` (feature)
- N-0059 **Stage 6.4: bulk-backfill completion UX (BACKLOG-060)** `proposed`/`proposed` (feature)
- N-0060 **Historical lineup backfill (years before 2025 Q1)** `blocked`/`blocked` (feature)
- N-0061 **Stage 7.1-7.3: promotion intelligence v2 (listing/competitor evidence, observed cover, comparable scope)** `proposed`/`proposed` (feature)
- N-0062 **Stage 7.5: supply Arrived state and plan-unit PO coverage (BACKLOG-178/179)** `proposed`/`proposed` (feature)
- N-0063 **Stage 7.4: uplift from settled claims (BACKLOG-185), data-gated** `blocked`/`blocked` (feature)
- N-0065 **BU entitlements: user-to-product-line mapping over the D-g lines** `blocked`/`blocked` (feature)
- N-0066 **Naming and polish pass across containers, with outside buyers in mind** `proposed`/`proposed` (feature)
- N-0067 **Light/dark theme completion** `proposed`/`proposed` (feature)
- N-0068 **Multi-catalogue column sets (BACKLOG-198)** `blocked`/`blocked` (feature)
- N-0069 **Stage 8: steward and import engine follow-ons (trigger-gated umbrella)** `proposed`/`proposed` (feature)
- N-0070 **Stage 10: multi-tenant productisation (P6)** `proposed`/`proposed` (feature)
- N-0071 **Stage 4.6: open->shipped fact double-count policy (BACKLOG-062), undecided** `blocked`/`blocked` (feature)
- N-0072 **Stage 4.7: ACZA workbook non-operational sheets allowlist (BACKLOG-046), undecided** `blocked`/`blocked` (feature)
