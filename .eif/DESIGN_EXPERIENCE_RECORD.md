---
eif: design-experience-record
version: 0.3
status: template
last_updated: 2026-08-29
owner: UX-003
review_after: 90d
provenance: design-direction
not_a_baseline: true
---

# Design experience record — {{audit_id}}

Material design intelligence artifact. **Not** an implementation baseline.
Optional file — not a second ledger. Programme quality dimensions on the node remain the gate.

## Artifact class (per node)

- node id:
- `target_artifact_class:` ia_concept | interaction | high_fidelity
- delivered `design_artifact_class`:
- sequence note: (this node only / follows prior ia_concept / follows interaction)

Do not infer class from free-text “high-fidelity” wording. An `ia_concept` record may omit identity tokens and execution slots (`artifact_class=ia_concept`). A `high_fidelity` record must not blanket-`na` those kinds.

## Materiality

- class: shell | redesign | module | explicit-request
- activation: design_experience facet / Audit Mode material scope

## Art direction (PROPOSAL)

- intended character:
- typography / density philosophy:
- recognisable DNA without logo/name?

## Divergence

| Direction | Philosophy / spatial / hierarchy difference | Status |
|---|---|---|
| A | | candidate / rejected / selected |
| B | | |
| C | | optional for module work |

Preservation rationale (if current direction retained):

## Design signatures (2–4 when appropriate)

1.
2.

## Identity tokens (`high_fidelity`)

Declared visual decisions — not a prescribed palette or typeface.

- direction_name:
- type_pairing:
- numeral_treatment:
- rule_weight:
- accent_limit:

## Interaction spec (`interaction` and `high_fidelity`)

- drill-down / context preservation:
- named actions / keyboard:
- domain-appropriate work surface (what the user actually works in):

## State coverage (`interaction` and `high_fidelity`)

Enumerated: populated | loading | empty | error | blocked | confirm | (others as the job requires)

## Execution decisions (`high_fidelity`)

Each slot: `applicable` with rationale/evidence, or `not_applicable` with rationale. Not dummy `na`.

| Slot | Status | Rationale / evidence |
|---|---|---|
| responsive_decision | | viewport/input intent; not accidental shrink |
| visualisation_decision | | only if causally a differentiator; not a chart mandate |
| consequential_action_decision | | preview / blocked / confirm where the action is consequential |

## Rendered comparison (when practical)

Declare `artifact_class` of what was compared (ia_concept vs high_fidelity).

| Criterion | Candidate A | Candidate B | Notes |
|---|---|---|---|
| task clarity | | | comparative judgment, not numeric score |
| operational density | | | |
| hierarchy | | | multi-channel, not pill-only |
| distinctiveness | | | visual vocabulary, not IA-only |
| state handling | | | |
| responsive behaviour | | | |

**Selected:** — **Rejected:** —

## CR-006 sameness review

- structural / IA patterns challenged:
- **visual-vocabulary** challenge (required on `high_fidelity` even when IA already differs):
- justified familiar patterns:

## Prototype voice (`high_fidelity`)

- surface copy is product-facing (not redesign meta-commentary):
- meta-commentary lives in this record / `notes.md`, not on the prototype:

## Cross-category principles (Volume 17)

| External domain | Principle | Causal value | Adaptation | Evidence |
|---|---|---|---|---|

## Operational excellence check

- repeated-task efficiency:
- scan / keyboard:
- edge states:

## Simulated journeys (UX-001)

- journey ids walked:
- failures visible despite green tests:
